#!/usr/bin/env bash
find ~/.password-store/pass/ -type f -iname '*.gpg' \
  | sed 's|/home/moz/.password-store/||g' \
  | sed 's|.gpg||g' \
  | rofi -dmenu \
  | xargs -r pass show \
  | head -n1 | tr -d '\r' | wl-copy
